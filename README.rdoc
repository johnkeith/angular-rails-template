1. rails new angular_rails_template -T -B --database=postgresql
2. gemfile
  group :test, :development do
    gem 'rspec'
    gem 'rspec-rails'
    gem 'factory_girl_rails'
    gem 'capybara'
    gem 'database_cleaner'
    gem 'selenium-webdriver'
  end

  gem 'bower-rails'
3.
